name: Snake Game Animation

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: jasonlong/cayley-theme-snake@v1.0.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
